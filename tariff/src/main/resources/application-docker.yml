spring:
  datasource:
    url: jdbc:postgresql://db:5432/default
    username: pgsql
    password: cs203g1t1
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true  #(cloud deployment; ensures that db is set up)
    validate-on-migrate: false

# external MS (point to real hosts later if you spin them up)
country:
# base-url: ${COUNTRY_MS_BASE:http://country:5005} # local ver
  baseUrl:   http://country:5005 # prod ver
  byNamePath: /api/countries/by-name

product:
# base-url: ${PRODUCT_MS_BASE:http://product:5002}
  baseUrl: http://product:5002
  lookupPath: /api/v1/hs-code/search

server:
  port: 5004
